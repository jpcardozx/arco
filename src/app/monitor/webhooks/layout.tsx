export const metadata = {
  title: 'Monitor de Webhooks | ARCO',
  description: 'Monitoramento em tempo real de webhooks do Mercado Pago',
};

export default function WebhookMonitorLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
